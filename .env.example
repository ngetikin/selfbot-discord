TOKEN=             # Discord token akun selfbot (jangan dibagikan). Bisa ambil dari DevTools > localStorage token.
VOICE_CHANNEL_ID=  # ID voice channel tujuan auto-join.
TARGET_GUILD_ID=   # ID server utama tempat bot beroperasi.
ADMIN_ROLE_IDS=    # ID role admin, pisahkan dengan koma jika lebih dari satu.
TTS_LANG=id-ID     # Locale TTS, default Bahasa Indonesia.
LOG_LEVEL=info     # Log level: debug|info|warn|error.
EMOJI_CHANNEL_IDS= # Comma-separated channel IDs for auto-emoji reactions.
MEME_CHANNEL_ID=   # Channel ID untuk daily meme (opsional).
MEME_API_URL=      # Opsional: override sumber meme (default candaan-api.vercel.app/api/receh/random).
MEME_DEBUG_NOW=    # Jika true, kirim meme sekali lagi saat start (untuk testing).
ACTIVITY_MESSAGES= # Comma-separated activities for rotation (opsional).
GROQ_API_KEY=      # API key Groq (OpenAI-compatible).
GROQ_MODEL=llama-3.2-1b-preview  # Default model untuk chat mention (paling ringan, ada fallback).
